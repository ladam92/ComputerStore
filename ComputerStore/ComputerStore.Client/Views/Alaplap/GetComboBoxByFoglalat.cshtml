@model List<ComputerStore.DTO.Types.Alaplap>

@Html.DropDownList("AlaplapID", new SelectList(Model, "ID", "MegnevezesEsAr"), "Kérem válasszon", new { @class="form-control", @onchange= "alaplapComboBoxChanged()", @id="alaplapComboBox" })

<script>
    function alaplapComboBoxChanged()
    {
        var id = $("#alaplapComboBox").val();

        $.ajax({
            url: "@Url.Action("GetGeposszarkoComboBoxes", "GepOsszerako")",
            type: "GET",
        data: {
            alaplapID: id,
            },
        success: function (response) {
            $("#dataDiv").html(response);

        },
        error: function (jqXHR, textStatus, errorThrown) {
            console.log(textStatus, errorThrown);
        }
    });
    }
</script>