@model ComputerStore.DTO.Types.PageableList<ComputerStore.DTO.Types.Monitor>

@{
    ViewBag.Title = "GetByTipus";
}

<div class="row">

    @foreach (var item in Model.Items)
    {
        <div class="col-sm-4 col-lg-4 col-md-4">
            <div class="thumbnail">
                <img height="300" src="@String.Concat("data:image/jpeg;base64,", Convert.ToBase64String(item.Kep))" alt="">
                <div class="caption">
                    <h4>
                        <a href="#" data-toggle="modal" data-target="@String.Concat("#monitorModel", item.ID)">@item.Megnevezes</a>
                    </h4>

                    <h4 class="pull-right">@String.Concat(item.NettoAr, " Ft")</h4>
                </div>
                @*<div class="ratings">
        <p class="pull-right">15 reviews</p>
        <p>
            <span class="glyphicon glyphicon-star"></span>
            <span class="glyphicon glyphicon-star"></span>
            <span class="glyphicon glyphicon-star"></span>
            <span class="glyphicon glyphicon-star"></span>
            <span class="glyphicon glyphicon-star"></span>
        </p>
    </div>*@
            </div>
        </div>

        <div id=@String.Concat("monitorModel", item.ID) class="modal fade" role="dialog" hidden>
            <div class="modal-dialog">

                <!-- Modal content-->
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                        <h4 class="modal-title">@item.Megnevezes</h4>
                    </div>
                    @*<div class="modal-body">
                        <p><pre>@item.Leiras</pre></p>
                    </div>*@
                    <div class="modal-footer">
                        <button type="button" class="btn btn-success" data-dismiss="modal">Kosárba</button>
                        <button type="button" class="btn btn-danger" data-dismiss="modal">Bezárás</button>
                    </div>
                </div>

            </div>
        </div>
    }
</div>

<div class="pageSelector">
    <button onclick="javascript:location.href='@Url.Action("GetByFelbontas", "Monitor", new { tipusId = ViewBag.CurrentID, pageNumber = Model.CurrentPage - 1 })'" @if (Model.CurrentPage == 1) { <text> disabled </text>    } class="btn btn-primary"><span class="glyphicon glyphicon-chevron-left" /></button>
    @String.Concat(Model.CurrentPage, "/", Model.MaxPages, " oldal")
    <button onclick="javascript:location.href='@Url.Action("GetByFelbontas", "Monitor", new { tipusId = ViewBag.CurrentID, pageNumber = Model.CurrentPage +1 })'" @if (Model.CurrentPage == Model.MaxPages) { <text> disabled </text>     } class="btn btn-primary"> <span class="glyphicon glyphicon-chevron-right" /></button>
</div>

